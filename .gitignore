# Build output
build/
dist/
out/

# CEF binary distribution (downloaded at configure time, never committed).
# Vendored header-only libraries under third_party/ (e.g. json) ARE committed;
# only the downloaded CEF distribution is ignored.
shell/third_party/cef/
shell/third_party/*.tar.bz2
shell/third_party/*.tar.bz2.sha1

# Node
node_modules/
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
