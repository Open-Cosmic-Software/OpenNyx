{
  "name": "opennyx-shell",
  "version": "0.1.0",
  "private": true,
  "description": "OpenNyx browser shell (M1) — Tauri v2 UI: tab strip, omnibox, command palette.",
  "license": "MIT",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
